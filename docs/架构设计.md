> 📖 **文档导航**：[项目概览](项目概览.md) · [架构设计](架构设计.md) · [模块详解](模块详解.md) · [学习路线](学习路线.md) · [开发指南](开发指南.md) · [依赖管理](依赖管理.md)

# 架构设计

> 📖 **文档导航**：[项目概览](项目概览.md) · [架构设计](架构设计.md) · [模块详解](模块详解.md) · [学习路线](学习路线.md) · [开发指南](开发指南.md) · [依赖管理](依赖管理.md)

## 一、系统架构图

系统架构图展示了项目的五大方向及其子方向，以及方向之间的工作流关系。数据从获取阶段逐步流向处理、可视化、建模，最终到达综合应用阶段，形成完整的分析闭环。

```mermaid
graph TD
    A["01_数据获取"]
    A1["01_网页基础"]
    A2["02_网络爬虫"]
    A3["03_数据库查询"]
    A4["04_API数据获取"]
    A --> A1
    A --> A2
    A --> A3
    A --> A4

    B["02_数据处理"]
    B1["01_NumPy"]
    B2["02_Pandas"]
    B3["03_数据清洗"]
    B4["04_特征工程"]
    B --> B1
    B --> B2
    B --> B3
    B --> B4

    C["03_数据可视化"]
    C1["01_Matplotlib"]
    C2["02_Seaborn"]
    C3["03_交互式可视化"]
    C4["04_地理空间分析"]
    C --> C1
    C --> C2
    C --> C3
    C --> C4

    D["04_分析建模"]
    D1["01_EDA方法"]
    D2["02_统计分析"]
    D3["03_监督学习"]
    D4["04_无监督学习"]
    D5["05_模型评估与优化"]
    D6["06_时序分析与预测"]
    D7["07_推荐系统"]
    D8["08_异常检测"]
    D9["09_AB测试与实验设计"]
    D10["10_深度学习基础"]
    D --> D1
    D --> D2
    D --> D3
    D --> D4
    D --> D5
    D --> D6
    D --> D7
    D --> D8
    D --> D9
    D --> D10

    E["05_综合应用"]
    E1["01_NLP基础"]
    E2["02_数据报告与导出"]
    E3["03_商业分析方法"]
    E4["04_数据工程与ETL"]
    E --> E1
    E --> E2
    E --> E3
    E --> E4

    A -->|原始数据| B
    B -->|结构化数据| C
    B -->|特征数据| D
    C -->|可视化洞察| D
    D -->|分析结果| E
```

## 二、模块依赖图

模块依赖图揭示了各子方向之间的技术依赖关系。Pandas 作为核心枢纽，向上依赖 NumPy，向下支撑数据清洗、特征工程、EDA、时序分析、NLP、推荐系统、AB 测试和报告导出等多个模块；Seaborn 同时依赖 Matplotlib 和 Pandas；特征工程的产出直接驱动监督学习和无监督学习建模；API 数据获取与 ETL 管道形成数据工程闭环；推荐系统和异常检测依赖监督学习与无监督学习的基础能力；深度学习基础以 NumPy 为底座并延伸自监督学习。

```mermaid
graph LR
    NumPy["01_NumPy"] --> Pandas["02_Pandas"]
    NumPy --> Matplotlib["01_Matplotlib"]
    NumPy --> DL["10_深度学习基础"]
    Pandas --> Seaborn["02_Seaborn"]
    Matplotlib --> Seaborn
    Pandas --> EDA["01_EDA方法"]
    Pandas --> DB["03_数据库查询"]
    Pandas --> Clean["03_数据清洗"]
    Pandas --> Feature["04_特征工程"]
    Pandas --> RecSys["07_推荐系统"]
    Pandas --> ABTest["09_AB测试与实验设计"]
    Feature --> Supervised["03_监督学习"]
    Feature --> Unsupervised["04_无监督学习"]
    EDA --> Supervised
    Supervised --> Eval["05_模型评估与优化"]
    Unsupervised --> Eval
    Supervised --> RecSys
    Unsupervised --> Anomaly["08_异常检测"]
    Supervised --> Anomaly
    Supervised --> DL
    Eval --> DL
    Pandas --> TimeSeries["06_时序分析与预测"]
    Pandas --> NLP["01_NLP基础"]
    Pandas --> Report["02_数据报告与导出"]
    Pandas --> Geo["04_地理空间分析"]
    API["04_API数据获取"] --> Pandas
    DB --> ETL["04_数据工程与ETL"]
    API --> ETL
    Clean --> ETL
    Pandas --> ETL
    Pandas --> Biz["03_商业分析方法"]
    EDA --> Biz
    Stats["02_统计分析"] --> ABTest
```

## 三、数据流图

数据流图描述了数据从外部来源到最终输出的完整流转路径。原始数据通过爬虫、API 接口或 SQL 查询获取后，经 NumPy/Pandas 进行处理与清洗，再通过可视化工具呈现洞察，随后进入建模分析阶段，最终以 NLP 应用、商业分析、数据报告或 ETL 管道的形式输出成果。

```mermaid
flowchart LR
    Source["数据源<br/>网页/数据库/API/文件"] --> Acquire["数据获取<br/>爬虫/SQL/API"]
    Acquire --> Process["数据处理<br/>NumPy/Pandas/清洗/特征工程"]
    Process --> Visual["数据可视化<br/>Matplotlib/Seaborn/交互式/地理空间"]
    Process --> Model["分析建模<br/>EDA/统计/ML/时序/推荐/异常/AB测试/深度学习"]
    Visual --> Model
    Model --> Apply["综合应用<br/>NLP/报告/商业分析/ETL"]
    Apply --> Output["输出<br/>报告/仪表盘/推荐/管道"]
```

## 四、技术栈关系图

技术栈关系图按方向分组展示了各子方向所涉及的核心库。每个子方向都有其专属的工具链，同时部分库（如 NumPy、Pandas）作为基础设施跨方向共享。

```mermaid
graph TB
    subgraph 数据获取
        A1["01_网页基础<br/>requests / httpx"]
        A2["02_网络爬虫<br/>Scrapy / BeautifulSoup / Selenium"]
        A3["03_数据库查询<br/>SQLAlchemy / sqlite3 / psycopg2"]
        A4["04_API数据获取<br/>requests / httpx / json"]
    end

    subgraph 数据处理
        B1["01_NumPy<br/>NumPy"]
        B2["02_Pandas<br/>Pandas"]
        B3["03_数据清洗<br/>Pandas / missingno"]
        B4["04_特征工程<br/>scikit-learn / Pandas"]
    end

    subgraph 数据可视化
        C1["01_Matplotlib<br/>Matplotlib"]
        C2["02_Seaborn<br/>Seaborn"]
        C3["03_交互式可视化<br/>Plotly / Bokeh / Pyecharts"]
        C4["04_地理空间分析<br/>GeoPandas / Folium / contextily"]
    end

    subgraph 分析建模
        D1["01_EDA方法<br/>Pandas / ydata-profiling"]
        D2["02_统计分析<br/>SciPy / statsmodels"]
        D3["03_监督学习<br/>scikit-learn / XGBoost"]
        D4["04_无监督学习<br/>scikit-learn"]
        D5["05_模型评估与优化<br/>scikit-learn / Optuna"]
        D6["06_时序分析与预测<br/>statsmodels / Prophet"]
        D7["07_推荐系统<br/>scikit-learn / SurPRISE / NumPy"]
        D8["08_异常检测<br/>scikit-learn / PyOD"]
        D9["09_AB测试与实验设计<br/>SciPy / statsmodels"]
        D10["10_深度学习基础<br/>PyTorch / NumPy"]
    end

    subgraph 综合应用
        E1["01_NLP基础<br/>jieba / NLTK / spaCy"]
        E2["02_数据报告与导出<br/>Jupyter / OpenPyXL / pdfkit"]
        E3["03_商业分析方法<br/>Pandas / Lifetimes / Scikit-learn"]
        E4["04_数据工程与ETL<br/>SQLAlchemy / Airflow / Pandas"]
    end
```

## 五、学习路径图

学习路径图展示了推荐的模块学习顺序及前置依赖关系。建议从网页基础和 NumPy 入门，逐步掌握 Pandas 后再学习数据清洗与特征工程；可视化方向从 Matplotlib 到 Seaborn 再到交互式可视化和地理空间分析；建模方向需先完成 EDA 和特征工程，再进入监督/无监督学习，之后可学习推荐系统、异常检测、AB 测试和深度学习基础；综合应用方向在掌握建模能力后学习商业分析方法，在掌握数据处理和获取后学习数据工程与 ETL。

```mermaid
graph TD
    WebBase["01_网页基础"] --> Crawler["02_网络爬虫"]
    Crawler --> API["04_API数据获取"]

    NumPy["01_NumPy"] --> Pandas["02_Pandas"]
    Pandas --> Clean["03_数据清洗"]
    Clean --> Feature["04_特征工程"]

    NumPy --> Matplotlib["01_Matplotlib"]
    Matplotlib --> Seaborn["02_Seaborn"]
    Seaborn --> Interactive["03_交互式可视化"]
    Matplotlib --> Geo["04_地理空间分析"]

    Pandas --> EDA["01_EDA方法"]
    EDA --> Stats["02_统计分析"]

    Pandas --> Feature
    Feature --> Supervised["03_监督学习"]
    Supervised --> Eval["05_模型评估与优化"]

    Feature --> Unsupervised["04_无监督学习"]
    Unsupervised --> Eval

    Supervised --> RecSys["07_推荐系统"]
    Unsupervised --> RecSys

    Unsupervised --> Anomaly["08_异常检测"]
    Supervised --> Anomaly

    Stats --> ABTest["09_AB测试与实验设计"]

    Supervised --> DL["10_深度学习基础"]
    NumPy --> DL

    Pandas --> TimeSeries["06_时序分析与预测"]

    Pandas --> NLP["01_NLP基础"]

    Pandas --> Report["02_数据报告与导出"]

    Pandas --> Biz["03_商业分析方法"]
    EDA --> Biz

    Pandas --> ETL["04_数据工程与ETL"]
    Clean --> ETL
    API --> ETL
```

## 六、用例图

用例图展示了三类角色与项目功能的交互关系。初学者主要关注学习与探索，通过浏览学习路线、运行案例代码和查看模块详解来掌握数据分析技能；数据分析师侧重于实际分析工作，使用 EDA、统计分析、AB 测试和商业分析方法产出数据报告；开发者则聚焦于项目贡献与协作，包括贡献新案例、修改代码、搭建开发环境和提交 PR。

```mermaid
graph LR
    subgraph 初学者
        Actor1((初学者))
    end
    subgraph 数据分析师
        Actor2((数据分析师))
    end
    subgraph 开发者
        Actor3((开发者))
    end

    UC1([浏览学习路线])
    UC2([运行案例代码])
    UC3([查看模块详解])
    UC4([使用EDA/统计分析])
    UC5([AB测试/商业分析])
    UC6([生成数据报告])
    UC7([贡献新案例])
    UC8([修改代码])
    UC9([搭建开发环境])
    UC10([提交PR])

    Actor1 --> UC1
    Actor1 --> UC2
    Actor1 --> UC3
    Actor2 --> UC4
    Actor2 --> UC5
    Actor2 --> UC6
    Actor3 --> UC7
    Actor3 --> UC8
    Actor3 --> UC9
    Actor3 --> UC10
```

## 七、组件图

组件图展示了5大方向作为组件的接口定义与依赖关系。01_数据获取提供「原始数据」接口，是整个数据流的起点；02_数据处理依赖原始数据并提供「结构化数据」接口，是核心枢纽；03_数据可视化依赖结构化数据，提供「可视化图表」接口；04_分析建模同时依赖结构化数据和可视化图表，提供「分析模型」接口；05_综合应用依赖分析模型和结构化数据，提供「业务产出」接口，是最终交付层。

```mermaid
graph LR
    subgraph 01_数据获取
        A_IF["«interface»<br/>原始数据"]
    end
    subgraph 02_数据处理
        B_IF["«interface»<br/>结构化数据"]
    end
    subgraph 03_数据可视化
        C_IF["«interface»<br/>可视化图表"]
    end
    subgraph 04_分析建模
        D_IF["«interface»<br/>分析模型"]
    end
    subgraph 05_综合应用
        E_IF["«interface»<br/>业务产出"]
    end

    A_IF -->|依赖| B_IF
    B_IF -->|依赖| C_IF
    B_IF -->|依赖| D_IF
    C_IF -->|依赖| D_IF
    D_IF -->|依赖| E_IF
    B_IF -->|依赖| E_IF
```

## 八、部署图

部署图展示了项目在本地开发环境的部署结构。开发者电脑作为唯一的执行节点，运行 Python ≥ 3.9 运行时环境；项目代码目录承载全部 211+ 案例文件；pip 依赖管理根据 requirements.txt 安装 23 个依赖包；部分案例使用 SQLite 内存数据库进行数据存储与查询；所有输出结果统一写入 output/ 目录。

```mermaid
graph TB
    DevPC["🖥️ 开发者电脑"]
    Python["Python ≥ 3.9 运行时"]
    CodeDir["项目代码目录<br/>01_数据获取 ~ 05_综合应用"]
    Pip["pip 依赖管理"]
    ReqTxt["requirements.txt<br/>23个依赖包"]
    SQLite["SQLite 内存数据库<br/>部分案例使用"]
    Output["output/ 输出目录"]

    DevPC --> Python
    Python --> CodeDir
    Python --> Pip
    Pip --> ReqTxt
    Python --> SQLite
    Python --> Output
```

## 九、ER图

ER图展示了项目中主要案例涉及的数据实体及其关系。推荐系统案例中，用户对物品产生评分；ETL案例中，客户下单购买商品形成订单；NLP案例中，文章包含多个关键词；异常检测案例中，交易记录独立存在并标注异常状态。这些实体关系覆盖了项目5大方向中典型的数据建模场景。

```mermaid
erDiagram
    User ||--o{ Rating : "给出"
    Item ||--o{ Rating : "收到"
    Customer ||--o{ Order : "下单"
    Order ||--|{ Product : "包含"
    Article ||--o{ Keyword : "包含"

    User {
        string user_id PK
        string name
        string email
    }
    Rating {
        string rating_id PK
        string user_id FK
        string item_id FK
        float score
        datetime created_at
    }
    Item {
        string item_id PK
        string name
        string category
    }
    Customer {
        string customer_id PK
        string name
        string region
    }
    Order {
        string order_id PK
        string customer_id FK
        datetime order_date
        float total_amount
    }
    Product {
        string product_id PK
        string name
        float price
        string category
    }
    Article {
        string article_id PK
        string title
        string content
        datetime publish_date
    }
    Keyword {
        string keyword_id PK
        string word
        float tfidf_score
    }
    Transaction {
        string transaction_id PK
        float amount
        string type
        datetime timestamp
        boolean is_anomaly
    }
```

## 十、活动图

活动图展示了典型数据分析工作流的步骤与分支。数据获取阶段根据来源不同分为爬虫、API、SQL三条路径，汇聚后进入数据处理（清洗与特征工程）；随后工作流分为两条分支——分支A侧重数据可视化与洞察发现，分支B侧重分析建模与模型迭代；模型评估未达标时需调参或换模型重新训练，达标后两条分支汇合进入综合应用，最终输出报告、推荐或管道。

```mermaid
flowchart TD
    Start([开始]) --> Acquire[数据获取]
    Acquire --> |爬虫| Clean1[数据处理 - 清洗]
    Acquire --> |API| Clean2[数据处理 - 清洗]
    Acquire --> |SQL| Clean3[数据处理 - 清洗]
    Clean1 --> Feature[数据处理 - 特征工程]
    Clean2 --> Feature
    Clean3 --> Feature
    Feature --> Branch{分支选择}
    Branch --> |分支A| Visual[数据可视化]
    Visual --> Insight[洞察发现]
    Insight --> Apply[综合应用]
    Branch --> |分支B| ModelTrain[模型训练]
    ModelTrain --> ModelEval[模型评估]
    ModelEval --> Pass{模型是否达标?}
    Pass --> |否| Tune[调参/换模型]
    Tune --> ModelTrain
    Pass --> |是| Apply
    Apply --> Output[输出报告/推荐/管道]
    Output --> End([结束])
```

## 十一、状态图

状态图展示了数据从获取到产出的完整状态变迁过程。原始数据经数据清洗/转换变为已清洗状态，再经特征工程变为已特征化状态；此后可进入已分析或已可视化状态，两者之间可相互转换；最终到达已产出状态，完成整个数据分析生命周期。每个状态转换都有明确的触发条件。

```mermaid
stateDiagram-v2
    [*] --> 原始数据: 数据获取完成
    原始数据 --> 已清洗: 数据清洗/转换
    已清洗 --> 已特征化: 特征工程
    已特征化 --> 已分析: 建模分析
    已特征化 --> 已可视化: 可视化呈现
    已可视化 --> 已分析: 可视化洞察驱动建模
    已分析 --> 已可视化: 分析结果可视化
    已分析 --> 已产出: 综合应用输出
    已可视化 --> 已产出: 报告/仪表盘生成
    已产出 --> [*]
